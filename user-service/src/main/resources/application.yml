server:
  port: 8081
  ssl:
    key-store: classpath:certs/user-service-keystore.p12
    key-store-password: changeit
    key-store-type: PKCS12
    key-alias: user-service
    trust-store: classpath:certs/user-service-truststore.p12
    trust-store-password: changeit
    trust-store-type: PKCS12
spring:
  application:
    name: user-service

eureka:
  client:
    service-url:
      defaultZone: https://eureka-server.demoapi.io:8761/eureka/
      
